apiVersion: v1
kind: Service
metadata:
  name: vjepa2-server
  namespace: vjepa2
  labels:
    app: vjepa2-server
    app.kubernetes.io/name: vjepa2-server
    app.kubernetes.io/part-of: vjepa2
spec:
  selector:
    app: vjepa2-server
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
  type: ClusterIP
